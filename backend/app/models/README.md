# models/

Database models will live here (e.g. via SQLAlchemy).

Planned first model: `InterviewSession` — see TODO.md, Phase 1.

This folder currently only contains `__init__.py` and is intentionally empty
otherwise until the database layer is set up.
