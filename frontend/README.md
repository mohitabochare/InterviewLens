# frontend/

Intentionally empty placeholder. Framework choice (React, Next.js, plain
HTML/JS, etc.) is deferred to Phase 3 — see `TODO.md` and
`PROJECT_CONTEXT.md` for reasoning.

When ready:
1. Scaffold your chosen framework's starter project inside this folder.
2. Point it at the backend (default: `http://127.0.0.1:8000`).
3. Confirm it can hit `/health` successfully as a smoke test.
